import { Button } from "./Button";

const meta = {
  title: "Components/Button",
  component: Button
};

export default meta;

export const Primary = {
  args: {
    label: "Save"
  }
};

export const Secondary = {
  args: {
    label: "Cancel"
  }
};
