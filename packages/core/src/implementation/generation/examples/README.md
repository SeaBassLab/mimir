# APS Generate Fixtures

Fixtures for `mimir generate` verification:

1. `react-exports`: package with exported React component, Storybook, and README section.
2. `missing-metadata`: package with exported component but no README/Storybook human metadata.
3. `existing-aps`: package with pre-existing `dist/aps` resources to validate `--force` safety.
